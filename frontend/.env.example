# Dev mode with Vite proxy can keep this as /api
VITE_API_BASE_URL=/api

# For deployed frontend calling deployed backend directly, use full URL:
# VITE_API_BASE_URL=https://your-backend-domain.com/api
